// The landing page is a deliberately fixed-dark experience (see plan discussion) —
// these mirror the Phase 1 theme tokens exactly (--primary, --accent, --success)
// but as literal hex/font-stack values, since this subtree leans heavily on the
// `${V}38`-style hex+alpha-suffix pattern that hsl(var(--x)) can't express.
export const V    = "#7C3AED";
export const VA   = "#A855F7";
export const G    = "#10B981";
export const BG   = "#050507";
export const DISP = "'Bricolage Grotesque', sans-serif";
export const BODY = "'Inter', system-ui, -apple-system, sans-serif";
export const MONO = "'DM Mono', 'Courier New', monospace";
